# Replay Greenfield Branch

This branch keeps design docs + Cursor skills/rules but **removes** the finished `Y_OPS_MONITOR_V2` implementation so a new agent must rebuild from the design document.

Do not restore `src/y_ops_monitor_v2.prog.abap` from other branches while running the test.

Protocol: `docs/guides/replay-quality-test.md`
